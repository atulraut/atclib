/*
* Reverse String
***/

#include <stdio.h>

char* revStr (char *);

int main () {
  char*str;
  str = NULL;					
  str = "LUTA";
  str = revStr (char *s);
  printf ("\nstr = %s\n", str);
  return 0;
}

char* revStr (char *s) {
  int i, j;
  char temp;
  i = 0;
  j = strlen(s);
  for (i; i<) {
    
  }
  printf ("\ns = %s\n", s);
  return s;
}
